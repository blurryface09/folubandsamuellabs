import { PageShell } from "@/components/app/page-shell";

export default function LeavePage() {
  return (
    <PageShell
      title="Leave"
      description="Leave balances, policy rules, requests, approval chains, and calendars will attach to this module."
    />
  );
}
