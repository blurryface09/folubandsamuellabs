import { PageShell } from "@/components/app/page-shell";

export default function EmployeesPage() {
  return (
    <PageShell
      title="Employees"
      description="Build employee profiles, employment lifecycle states, compensation links, and organization-scoped directory views here."
    />
  );
}
