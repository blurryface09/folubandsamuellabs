import { PageShell } from "@/components/app/page-shell";

export default function AttendancePage() {
  return (
    <PageShell
      title="Attendance"
      description="Prepare clock-in records, shift calendars, exceptions, and tenant-scoped attendance policy workflows."
    />
  );
}
