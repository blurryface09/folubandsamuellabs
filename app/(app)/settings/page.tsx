import { PageShell } from "@/components/app/page-shell";

export default function SettingsPage() {
  return (
    <PageShell
      title="Settings"
      description="Organization profile, roles, access control, billing, integrations, and tenant configuration start from this route."
    />
  );
}
