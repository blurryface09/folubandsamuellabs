import { PageShell } from "@/components/app/page-shell";

export default function PayrollPage() {
  return (
    <PageShell
      title="Payroll"
      description="Payroll runs, payslips, compensation records, statutory deductions, and approval checkpoints will live here."
    />
  );
}
